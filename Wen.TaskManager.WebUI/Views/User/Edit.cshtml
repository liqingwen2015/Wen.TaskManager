@model Wen.TaskManager.WebUI.Models.UserEditViewModel

@{
    ViewBag.Title = "Index";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            首页
            <small></small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li><a href="#">Tables</a></li>
            <li class="active">Simple</li>
        </ol>
    </section>
    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="box">
                    <div class="box-header with-border">
                        <h3 class="box-title">Horizontal Form</h3>
                    </div>
                    <!-- /.box-header -->
                    @using (Html.BeginForm("Edit", "User", FormMethod.Post, new { @class = "form-horizontal" }))
                    {
                        @Html.HiddenFor(x=>x.UserId)
                        <div class="box-body">
                            @Html.ValidationSummary()
                            <div class="form-group">
                                <label for="" class="col-sm-1 control-label">用户名</label>

                                <div class="col-sm-3">
                                    @Html.TextBoxFor(x => x.Name, new { @class = "form-control", placeholder = Model.Name })
                                </div>
                            </div>
                            @*<div class="form-group">
                                <label for="" class="col-sm-1 control-label">密码</label>

                                <div class="col-sm-3">
                                    @Html.TextBoxFor(x => x.Password, new { @class = "form-control", type = "password", placeholder = "密码" })
                                </div>
                            </div>*@
                            <div class="form-group">
                                <label class="col-sm-1 control-label">角色</label>
                                <div class="col-sm-6">
                                    <div class="checkbox">
                                        @if (Model.Roles != null)
                                        {
                                            foreach (var role in Model.Roles)
                                            {
                                                <label>
                                                    @if (Model.RoleIds == null || !Model.RoleIds.Contains(role.Id))
                                                    {
                                                        <input name="RoleIds" type="checkbox" value="@role.Id" />
                                                        @role.Name
                                                    }
                                                    else
                                                    {
                                                        <input name="RoleIds" type="checkbox" value="@role.Id" checked="checked" />
                                                        @role.Name
                                                    }
                                                </label>
                                            }
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- /.box-body -->
                        <div class="box-footer">
                            @*<button type="submit" class="btn btn-default">Cancel</button>*@
                            <button type="submit" class="btn btn-info pull-right">保存</button>
                        </div>
                            <!-- /.box-footer -->
                    }
                    <!-- /.box-body -->

                </div>
            </div>
        </div>
    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->

<script>

</script>